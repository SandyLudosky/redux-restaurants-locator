import styled from 'styled-react-native'
import { View } from 'react-native'
export const ButtonRefresh = styled(View)(() => {
    return {
        width: 100,
        padding: 2, 
        fontSize: 8, 
        textDecorationLine: 'underline',
        backgroundColor: 'transparent',
    }
})
